name: ddc
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - numpy
    - matplotlib
    - pandas
    - --extra-index-url https://download.pytorch.org/whl/cu124
    - torch==2.5.1
